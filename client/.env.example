VITE_API_URL=http://localhost:5000/api/v1
VITE_STRIPE_PUBLISHABLE_KEY=pk_test_replace_me

# Set to "false" once the backend is running and you want to hit the real API.
# While "true" (default), auth uses in-memory mock data so the UI can be exercised.
VITE_USE_MOCK=true
